# Required: your Discord bot token (Developer Portal > your app > Bot > Reset Token)
DISCORD_BOT_TOKEN=your-token-here

# Optional: a guild (server) ID. When set, slash commands are registered only in that
# server and appear instantly (great for development). When unset, commands are
# registered globally, which can take a few minutes to propagate.
GUILD_ID=
